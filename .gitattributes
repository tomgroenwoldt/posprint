# These are executed on Linux. A CRLF checkout on Windows would round-trip back
# as CRLF and break them with "bad interpreter: /usr/bin/env bash^M".
*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
